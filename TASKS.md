# BENCH-01 可执行任务书

## 目标
- 前端查询页和管理页分离，前台不要直接暴露管理能力。
- 文件结构尽量对齐 `D:\项目\blog` 的分层风格：页面、样式、脚本、组件、数据各自分开。
- 整体 UI 改亮、改干净，参考 `https://blog.motues.top/` 的轻量卡片化风格，但不要硬抄博客内容。
- 继续支持 GitHub Pages 部署，保留现有核心能力：查询、筛选、新增/编辑、数量调整、导入/导出、数据手册链接。

## 参考对象
- 本地参考项目：`D:\项目\blog`
- 在线参考风格：`https://blog.motues.top/`
- 当前工程：`D:\项目\元器件管理`

## 范围
- 必须保留现有字段：`name/category/model/package/quantity/location/datasheet/notes`。
- 必须保留现有数据能力：`localStorage` 读写、JSON 导入导出、可选 Gist 同步、离线可用。
- 不允许破坏现有仓库历史，不要删除用户已有数据逻辑，只做结构迁移和界面升级。
- 不要求做后端或数据库，继续纯静态前端方案，适配 GitHub Pages。

## 设计方向
- 前台使用明亮奶油/米白底色，搭配单一主色做强调。
- 卡片、面板、按钮统一圆角，减少重阴影，改用轻边框和微阴影。
- 去掉 UI 里的 emoji 图标，统一使用简洁 SVG/文字按钮。
- 管理页做成独立 shell：顶部栏 + 侧边栏/标签 + 内容区，登录后再显示管理功能。
- 两套页面共享同一套基础样式变量，避免样式割裂。

## 任务清单

### 1. 梳理并重构目录
- 新建 `admin/` 作为独立管理页目录。
- 新建 `src/` 放前端逻辑；`styles/` 放样式；`data/` 放静态配置。
- 拆分 `index.html` 为前台查询壳页面。
- 新增 `admin/index.html` 作为管理页壳页面。
- 清理临时散乱文件和测试痕迹，保留正式页面。

### 2. 实现前台查询页
- 继承现有查询能力：搜索、分类/封装筛选、库存状态筛选、排序。
- 保留列表 + 详情双栏布局，优化为更轻盈的卡片/列表风格。
- 保留新增、编辑、数量调整、复制、删除、数据手册链接打开。
- 保留导入/导出、设置、Gist 同步入口。
- 优化空状态、加载反馈和移动端适配。

### 3. 实现管理页
- 管理页独立路由，不要和前台查询页混在同一个 HTML 里。
- 管理页包含登录校验；未登录只能看到登录界面。
- 登录后支持标签切换，至少包含：元器件管理、系统设置、帮助说明。
- 管理页内再做数据维护，而不是前台直接维护。
- 管理页也要继承新的明亮 UI，但比前台更偏工具型密度。

### 4. UI/UX 升级
- 重写配色变量，默认走明亮主题，深色主题只作为可选主题保留。
- 统一组件样式：按钮、输入框、选择器、表格/列表、弹窗、提示条。
- 提升对比度和层级：主按钮更强、弱化文字保留可读性、边框更清晰。
- 加入更克制的动效和过渡，保持工具站的效率感。
- 规范移动端：底部导航、顶部操作区、表单触控尺寸。

### 5. GitHub Pages 部署
- 确认前台访问路径和管理页访问路径在项目站点下都能正常打开。
- 更新仓库默认分支和 Pages 设置说明，确保可直接部署。
- 若管理页需要单独入口，统一为 `/admin/`。

### 6. 提交与清理
- 完成结构迁移和 UI 升级后提交 git。
- 删除临时文件、备份文件、旧版本测试 HTML、构建缓存。
- 提交信息要写清楚：结构重构、UI 升级、页面分离。

## 交付物
- `index.html`
- `admin/index.html`
- `styles/variables.css`
- `styles/components.css`
- `src/shared.js`
- `src/query.js`
- `src/admin.js`
- `README.md`
- 无多余临时文件

## 验收标准
- 前台查询页和管理页路径分离，视觉上明显不同。
- 前台可以直接浏览和查询；管理页必须登录后才可用。
- UI 明显比旧版更亮、更整洁，参考 `blog.motues.top` 的轻量质感。
- 移动端可用，无明显元素挤压或重叠。
- GitHub Pages 可直接部署，公开访问前台，管理页可进入。

## 执行顺序
1. 先建目录和共享样式变量。
2. 再做前台查询页并验证功能。
3. 再做管理页并验证登录和权限。
4. 统一美化两套页面的 UI。
5. 最后提交并清理临时文件。
